pipeline {
    agent { label 'slavery-node' }
    environment {
        registry = 'https://registry.gitlab.com'
        personalAccessToken = 'personal-access-token-api'
    }
    stages {
        stage('clone stage') {
            steps {
                checkout scmGit(branches: [[name: '*/main']], extensions: [], userRemoteConfigs: [[credentialsId: personalAccessToken, url: 'https://lytiendung.10@gitlab.com/lytiendung.10/project-01.git']])
            }
        }
        stage('build stage') {
            steps {
                sh 'id'
                sh "docker login -u lytiendung.10 -p glpat-NdGneVyHNL6R73VwSBdU ${registry}"
                script {
                    def dockerImage = docker.build("registry.gitlab.com/lytiendung.10/project-01:built-${env.BUILD_ID}")
                    script {
                        docker.withRegistry( registry, personalAccessToken ) {
                            dockerImage.push()
                        }
                    }
                }
            }
        }
    }
}


// stage ('Push image') {
//     withCredentials ( [usernamePassword ( credentialsId: 'gitlab-registry-credentials', usernameVariable: 'USER', passwordVariable: 'PASSWORD')]) {
//         def registry_url = "registry.example.com/"
//         bat "docker login -u $USER -p $PASSWORD ${registry_url}"
//         docker.withRegistry ("https://${registry_url}", "gitlab-registry-credentials") {
//             // Push your image now
//             bat "docker push registry.example.com/group/project/image"
//         }
//     }
// }